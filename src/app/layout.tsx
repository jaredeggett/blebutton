import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: "BLEButton | Physical Trigger for AI Workflows and Edge Automation",
  description:
    "Battery-free, kinetic energy-harvesting BLE 5.0 button. Press a button, trigger an AI prompt. No hub, no cloud, no batteries. Works with Jetson, Pi, ESP32, Home Assistant. MIT-licensed SDKs.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
